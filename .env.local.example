# Local-only Aurora config. Copy to `.env.local` and keep it uncommitted.

# Optional non-production test pilots. Comma separate multiple emails.
TEST_ACCOUNT_EMAILS="sgwoods@gmail.com,waskesiuhobiecat@gmail.com"
# Add real Supabase user ids here when known so those pilots can be filtered from shared score views.
TEST_ACCOUNT_USER_IDS=""
